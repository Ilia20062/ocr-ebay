{"containerOverrides": [{"name": "app", "command": ["node", "-e", "const { Client } = require('pg');\nconst uid = 'e498c438-c0e1-7076-92fe-1892efa8c083';\n(async () => {\n  const c = new Client({\n    host: process.env.PGHOST,\n    port: parseInt(process.env.PGPORT || '5432', 10),\n    user: process.env.PGUSER,\n    password: process.env.PGPASSWORD,\n    database: process.env.PGDATABASE,\n    ssl: { rejectUnauthorized: false },\n  });\n  await c.connect();\n  const p = await c.query('SELECT email FROM profiles WHERE id=$1', [uid]);\n  if (!p.rowCount) { console.error('WIPE: no profile ' + uid); process.exit(1); }\n  console.log('WIPE email=' + p.rows[0].email);\n  try {\n    await c.query('BEGIN');\n    const d = async (sql) => (await c.query(sql, [uid])).rowCount;\n    const r1 = await d('DELETE FROM retry_queue WHERE entity_id IN (SELECT id FROM images WHERE user_id=$1)');\n    const r2 = await d('DELETE FROM retry_queue WHERE entity_id IN (SELECT id FROM listings WHERE user_id=$1)');\n    const r3 = await d('DELETE FROM retry_queue WHERE entity_id IN (SELECT ps.id FROM product_searches ps JOIN upload_batches ub ON ub.id=ps.batch_id WHERE ub.user_id=$1)');\n    const a = await d('DELETE FROM audit_logs WHERE user_id=$1');\n    const l = await d('DELETE FROM listings WHERE user_id=$1');\n    const s = await d('DELETE FROM upload_sessions WHERE user_id=$1');\n    const b = await d('DELETE FROM upload_batches WHERE user_id=$1');\n    const im = await d('DELETE FROM images WHERE user_id=$1');\n    await c.query('COMMIT');\n    console.log('WIPE done retry=' + (r1 + r2 + r3) + ' audit=' + a + ' listings=' + l + ' sessions=' + s + ' batches=' + b + ' images=' + im);\n  } catch (e) {\n    await c.query('ROLLBACK').catch(() => {});\n    console.error('WIPE FAILED ' + e.message);\n    process.exit(1);\n  } finally {\n    await c.end();\n  }\n})();\n"]}]}